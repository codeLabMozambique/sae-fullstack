server:
  port: 8084

spring:
  application:
    name: sae-quiz-service
  datasource:
    url: jdbc:mysql://localhost:3306/ecarta_hub?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root123
  jpa:
    hibernate:
      ddl-auto: update
